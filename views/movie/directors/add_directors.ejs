<%- include('../../partials/header.ejs') %>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Director</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/antd/4.16.13/antd.min.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</head>
<body>
  <div class="mt-5" style="margin: 8%; padding-top: 5%;">
    <h2>Add Director</h2>
    <form id="addDirectorForm">
      <div class="form-group">
        <label for="name">Name</label>
        <input type="text" class="form-control" id="name" name="name" required>
      </div>
      <div class="form-group">
        <label for="image">Image URL</label>
        <input type="text" class="form-control" id="image" name="image" required>
      </div>
      <button type="submit" class="btn btn-primary">Add Director</button>
    </form>
  </div>

  <script>
    $(document).ready(function() {
      $('#addDirectorForm').submit(function(event) {
        event.preventDefault(); // Prevent default form submission

        // Gather form data
        var formData = {
          name: $('#name').val(),
          image: $('#image').val()
        };

        // Send an AJAX POST request to server (you need to handle this on server-side)
        $.ajax({
          type: 'POST',
          url: '/directors/add', // Update the URL to your server endpoint
          data: formData,
          success: function(response) {
            alert('Director added successfully!');
            // Optionally, you can redirect or update UI after successful addition
            // window.location.href = '/directors'; // Redirect to directors page
          },
          error: function(err) {
            console.error('Error adding director:', err);
            alert('Failed to add director. Please try again.');
          }
        });
      });
    });
  </script>
</body>
</html>
